# Kioku environment.
# Copy to .env.local and fill in the keys you need.

# Required for the "Ask your notes" Q&A feature.
OPENAI_API_KEY=

# Local SQLite database (relative paths resolved from project root).
DATABASE_URL=file:./kioku.db
